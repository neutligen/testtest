<%= link_to gravatar_for(current_user, size:50), current_user %>
  <h1><%= current_user.email[/[a-z\d\w\-.]+/,0] %></h1>
  <span>
    <% if current_user.admin? %>
  　　<%= link_to "ユーザ管理画面へ", current_user %>
    <% end %>
  </span>
  <span>総登録数：<%= current_user.to_do_lists.count %>件</span>